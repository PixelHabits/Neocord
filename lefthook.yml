pre-commit:
  parallel: true
  commands:
    ruff-format:
      glob: "**/*.py"
      run: cd backend && bunx ruff format {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    ruff-check:
      glob: "**/*.py"
      run: cd backend && bunx ruff check {push_files}